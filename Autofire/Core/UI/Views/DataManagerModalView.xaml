<UserControl x:Class="Autofire.Core.UI.Views.DataManagerModalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
			 xmlns:views="clr-namespace:Autofire.Core.UI.Views">

	<StackPanel Margin="16">
		<TextBlock x:Name="Title" />

		<TextBox Margin="0 8 0 0" HorizontalAlignment="Stretch"
				 materialDesign:HintAssist.Hint="Name"
				 Text="{Binding Name,
					UpdateSourceTrigger=PropertyChanged,
					NotifyOnValidationError=True,
					ValidatesOnDataErrors=True}"/>

		<TextBox Margin="0 14 0 0" HorizontalAlignment="Stretch"
				 materialDesign:HintAssist.Hint="Description"
				 Text="{Binding Description,
					UpdateSourceTrigger=PropertyChanged,
					NotifyOnValidationError=True,
					ValidatesOnDataErrors=True}"/>

		<views:ModalBase></views:ModalBase>
	</StackPanel>
</UserControl>
